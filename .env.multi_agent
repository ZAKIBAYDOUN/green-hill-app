# Green Hill Canarias Multi-Agent Configuration
# Copy to .env and customize as needed

# Deployment Mode
# simple: Single answer_question node (current cloud deployment)
# multi_agent: Full 7-agent system with digital twin orchestrator
DEPLOYMENT_MODE=simple

# Green Hill GPT Integration
# Set these to enable continuous testing against Green Hill GPT
# GREEN_HILL_GPT_URL=https://your-green-hill-gpt-endpoint.com/api/chat
# GREEN_HILL_API_KEY=your_api_key_here

# OpenAI Configuration (optional - has fallback)
# OPENAI_API_KEY=your_openai_key
# OPENAI_CHAT_MODEL=gpt-4o

# Continuous Testing
# Set to "true" to run tests in a loop
# CONTINUOUS_MODE=false
# TEST_INTERVAL=300

# Development Settings
# LANGCHAIN_TRACING_V2=true
# LANGCHAIN_API_KEY=your_langsmith_key
